port: 3000

postgresql:
  auth:
    database: "wlosiara-pl-auth-service"
    username: "products-service"
  primary:
    initdb:
      scripts-configmap: "wlosiara-pl-auth-service-initdb-scripts"

image:
  dockerRegistry:
    host: null # required
